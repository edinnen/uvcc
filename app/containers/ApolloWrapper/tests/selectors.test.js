// import { fromJS } from 'immutable';
// import { selectApolloWrapperDomain } from '../selectors';

describe('selectApolloWrapperDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
